This bundle keeps only the minimum configuration set needed to run the code:

- `base.yaml`: generic CPU-only RAG config for standard runs
- `legacy_acbsc_eval/hotpot_agnostic_acb_sc_full7405_p4.yaml`: frozen ACB-SC example config

The larger experimental config matrix has been intentionally excluded from the GitHub bundle.
