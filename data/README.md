Datasets are not bundled.

Expected behavior:
- HotpotQA / 2WikiMultihop / Natural Questions / SQuAD-open are downloaded on demand via `datasets`.
- BEIR corpora are downloaded on demand when running retrieval benchmarks.

Keep downloaded data out of Git.
